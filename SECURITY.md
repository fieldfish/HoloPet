# 安全策略

## 报告问题

请不要在公开 Issue 中发布密钥、录音、完整对话、数据库、设备 IP 或可识别个人身份的信息。公开仓库启用后，请优先使用 GitHub 的 Private vulnerability reporting；在该功能不可用时，先提交不含细节的联系请求。

## 支持范围

安全修复优先覆盖当前公开版本。历史内部交付包不属于公开支持范围。

## 凭据泄露

如果凭据已经进入提交历史，仅删除当前文件不够：应立即在服务商侧撤销或轮换凭据，并从 Git 历史中清除相关对象。
